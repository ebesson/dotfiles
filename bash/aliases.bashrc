#!/bin/bash
# some more ls aliases

alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias ls='ls -GFlh'
alias gzip=pigz
alias ij='open -b com.jetbrains.intellij'
alias wip='git wip'
alias amend='git amend'
alias got='git'
alias mci='mvn clean install'
alias ta='tmux a -t'
alias ddig='dig axfr local | grep '
